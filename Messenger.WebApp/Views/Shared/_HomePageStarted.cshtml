@using System.Security.Claims


<div class="d-flex flex-column justify-content-center text-center h-100 w-100" id="mainView">
    <div class="container">
        @*   <div class="avatar avatar-lg mb-2">
            <img class="avatar-img" src="../assets/media/avatar/4.png" alt="">
        </div> 

        <p class="text-info">
            UserId: @User.Claims.FirstOrDefault(c => c.Type == "UserId")?.Value
        </p>
        <p class="text-info">
            Name Family: @User.Claims.FirstOrDefault(c => c.Type == "NameFamily")?.Value
        </p>
        <p class="text-info">
            Role Name: @User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Role)?.Value
        </p>


        <input type="submit" id="logout-button" class="btn btn-outline-warning exit-tab-btn" value="خروج از سایت" />
 *@

        <h5>خوش آمدی <span>@User.Claims.FirstOrDefault(c => c.Type == "NameFamily")?.Value</span>!</h5>
        <p class="text-muted">لطفاً یک چت را برای شروع انتخاب کنید.</p>

    </div>
</div>