@{
    // The default layout is now handled by each view individually.
    // For example, Login.cshtml specifies its own layout,
    // and Index.cshtml specifies _ChatzyLayout.
    // This file can be left empty or we can set a general fallback if needed.
    Layout = null;
}
